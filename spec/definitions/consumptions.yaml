type: object
properties:
  id:
    type: integer
    format: int32
  client:
    $ref: '#/definitions/clients'
  piece:
    $ref: '#/definitions/pieces'
  qte:
    type: integer
    format: int32
  ca:
    type: number
    format: float
  dateVente:
    type: string
    format: date-time
